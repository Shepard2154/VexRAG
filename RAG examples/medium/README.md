# Medium RAG Examples

The `medium` folder contains RAG demos with external vector storage and larger retrieval corpora than `small`.

- `rag_02_nq_chroma_en` — Natural Questions-style QA with passages stored in **ChromaDB** (persistent).
